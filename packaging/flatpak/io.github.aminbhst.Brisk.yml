id: io.github.aminbhst.Brisk
runtime: org.freedesktop.Platform
runtime-version: '24.08'
sdk: org.freedesktop.Sdk
command: brisk
finish-args:
  - --socket=fallback-x11
  - --socket=wayland
  - --share=network
  - --share=ipc
  - --device=dri
  - --filesystem=xdg-download
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.freedesktop.secrets
  - --talk-name=org.kde.StatusNotifierWatcher
  - --system-talk-name=org.freedesktop.Avahi.*
  - --filesystem=xdg-documents
  - --filesystem=xdg-run/pipewire-0:ro

modules:
  - shared-modules/libayatana-appindicator/libayatana-appindicator-gtk3.json

  - name: gnome-common
    sources:
      - type: archive
        url: https://download.gnome.org/sources/gnome-common/3.18/gnome-common-3.18.0.tar.xz
        sha256: 22569e370ae755e04527b76328befc4c73b62bfd4a572499fde116b8318af8cf

  - name: libkeybinder
    build-commands:
      - ./autogen.sh --prefix=/app
      - make
      - make install
    sources:
      - type: git
        url: https://github.com/kupferlauncher/keybinder.git
    cleanup:
      - /include
      - /lib/pkgconfig
      - /share/gtk-doc
      - '*.la'

  - name: brisk
    buildsystem: simple
    build-commands:
      - install -Dm644 brisk/data/flutter_assets/assets/icons/logo.png /app/share/icons/hicolor/128x128/apps/io.github.aminbhst.Brisk.png
      - install -Dm644 brisk/brisk.desktop /app/share/applications/io.github.aminbhst.Brisk.desktop
      - install -dm755 /app/bin /app/brisk
      - cp -R brisk/ /app
      - ln -s /app/brisk/brisk /app/bin/brisk
      - /usr/bin/gtk-update-icon-cache /app/share/icons/hicolor || true

    sources:
      - type: dir
        path: .

